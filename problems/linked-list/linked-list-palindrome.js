// Given a singly linked list of nodes that contain string characters
// with only a head property, find out if characters spell out a palindrome

// val: 'a' next -> val: 'b' next -> vale 'a' next -> null

// add previous property to each node
// split list flip one side and compare

// I - LL
// O - Boolean if LL is palindrome
// C - constant space, time 0(n)
// E
