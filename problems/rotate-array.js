const rotateArray = matrix => {
  
  // iterate over array
  // select item at row 0, col 0
  // save to variable
  // 

  return matrix

};

module.exports = rotateArray;

//[[1,2], [3,4]] =>  [[3,1],[4,2]];