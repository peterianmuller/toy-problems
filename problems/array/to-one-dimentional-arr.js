const toLinearArr = matrix => {
	// keep track of topRow, bottomRow, leftCol, and rightCol
	let topRow = 0,
		bottomRow = matrix.length - 1,
		leftCol = 0,
		rightCol = matrix[0].length,
		copy = [];
	return copy;
};

module.exports = toLinearArr;
